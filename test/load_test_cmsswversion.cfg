# 

process TEST = {

	path p = { get }
        

	es_source = PoolDBESSource { VPSet toGet = {
                                   {string record = "DTReadOutMappingRcd"
                                     string tag = "cmssw_version"
                                    } }
		    		    bool loadAll = true
                                    string connect = "sqlite_file:testfile.db"
                                    untracked string catalog = "file:PoolFileCatalog.xml"
			            string timetype = "runnumber" 
                                    untracked uint32 authenticationMethod = 0
                                    untracked uint32 messagelevel = 0
				   }
	

	source = EmptySource {untracked int32 maxEvents = 100 untracked uint32 firstRun = 1 untracked uint32 numberEventsInRun = 10}

	module get = EventSetupRecordDataGetter { VPSet toGet = {
	       {string record = "DTReadOutMappingRcd"
	        vstring data = {"DTReadOutMapping"} } 
	       }
	       untracked bool verbose = true 
	}
        module print = AsciiOutputModule { }
        endpath ep = { print }
}
