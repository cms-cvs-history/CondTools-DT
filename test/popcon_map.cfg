process TEST = {
include "CondCore/DBCommon/data/CondDBCommon.cfi"
replace CondDBCommon.connect = "sqlite_file:test.db"
replace CondDBCommon.timetype = "runnumber"


  service = PoolDBOutputService {
    using CondDBCommon
    VPSet toPut = { { string record = "DTReadOutMappingRcd"
                      string tag = "map_test" } }
    untracked string logconnect = "sqlite_file:log.db"
  }
  source = EmptyIOVSource {
    string timetype = "runnumber"
    untracked uint32 firstRun = 1
    untracked uint32 lastRun = 1
    uint32 interval = 1
  }

  module t0 = DTReadOutMappingPopConAnalyzer {
    string record = "DTReadOutMappingRcd"
    untracked string name = "DTReadOutMapping"
    untracked bool loggingOn = true
    bool SinceAppendMode = true
    bool debug = false
    PSet Source ={
      string tag = "map_test"
      string file = "mapAll.txt"
      uint32 run = 1200
    }
  }

  path p = { t0 }

}
